window.FP_VARIANT = {
  activeBrushId: 'crayon',
  frameMode: false,
  clearOnly: true,
  // No tools rendered in the tools column/row — bgFill is replaced by the
  // page-bg toggle button in Save's slot (see app.js renderStrip). The
  // tools area being empty lets the page-picker grid extend into it.
  toolOrder: [],
};
